version: 0.2.1.{build}.{branch}-alpha
pull_requests:
  do_not_increment_build_number: true
clone_depth: 3
cache:
  - '%LocalAppData%\NuGet\Cache -> paket.lock'
test: off
build_script:
- cmd: build.cmd Release
artifacts:
  - path: '.build\nugets\*.nupkg'
    name: nuget
deploy: off